import React from 'react';
import { SafeAreaView, View, Text } from 'react-native';

const Detail = () => {
    return(
        <SafeAreaView>
            <View>
                <Text>Products</Text>
            </View>
        </SafeAreaView>
    );
};

export default Detail;